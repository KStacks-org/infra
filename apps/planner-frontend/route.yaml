apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: planner-route
  namespace: default
spec:
  parentRefs:
    - name: kstacks-gateway
  hostnames:
    - "planner.kstacks.org"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: planner-frontend-service
          port: 80
